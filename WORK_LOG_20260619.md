# Work Log — 2026-06-19

## Done today

- Built `train_eval_pusht.ipynb` — full end-to-end pipeline:
  - DINOv2 ViT-S/14 (frozen) → ConvAdapter (384→128, 16×16→4×4) → 2D pos emb → 1-layer Transformer
  - Multi-step rollout MSE training, cosine-distance MPC cost
  - CEMSolver (200 samples, 10 iters, horizon=10) + WorldModelPolicy
  - Cells: collect → load → define model → train (30 epochs) → eval → baseline compare → visualize

## Next session TODOs

- [ ] **Run the notebook on RunPod** — spin up a GPU pod (RTX 4090 for training run)
  - `cd /workspace/swm-runpod && jupyter notebook train_eval_pusht.ipynb`
  - Or in tmux: `jupyter nbconvert --to notebook --execute train_eval_pusht.ipynb --output train_eval_pusht_out.ipynb`
- [ ] **Check training convergence** — loss should drop from ~0.1→<0.01 within 30 epochs; if not, tune LR or num_steps
- [ ] **Compare eval vs baseline** — WM policy success rate vs random; any improvement validates the pipeline works
- [ ] **If success rate is low** — try:
  - Increase `num_samples` (200→500) or `n_steps` (10→20) in CEMSolver
  - Increase `episodes` in data collection (100→500)
  - Increase `NUM_EPOCHS` (30→100) or `NUM_STEPS` (16→32)
- [ ] **Save checkpoint to S3** after a good training run: `aws s3 cp pusht_worldmodel.pt s3://swm-research/checkpoints/`
- [ ] **If pipeline is solid** — next architecture experiments:
  - Unfreeze DINOv2 top layers (fine-tune)
  - Deeper Transformer (2–4 layers)
  - Add proprio/state conditioning alongside pixels
